const fs = require('fs');

// create test file with writeFile
 /*fs.writeFile("./text","hi this is a test from fs",(err) => {
     if (err) {
         console.error("Oh shit we have a error: " + err.message);
     } else {
         console.log("Oh yeah, success!");
     }
});*/

/* Appending the text to the file. */
/*fs.appendFile("./text","\nhi this is another test from fs\n",(err, data) => {
    if (err) {
        console.error("Oh shit we have a error: " + err.message)
    } else {
        console.log("Oh yeah, success!")
    }
});*/

/* Reading the file and printing it out. */
/*fs.readFile("./text",{},(err, data) => {
    if (err) {
        console.error("Oh shit we have a error: " + err.message)
    } else {
        console.log(data.toString())
    }
});

console.log("wait!");*/

